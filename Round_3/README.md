
  # Identify the Imposter Sensor

  This is a code bundle for Identify the Imposter Sensor. The original project is available at https://www.figma.com/design/pvi0FWkpGIcOdkuUoiphWZ/Identify-the-Imposter-Sensor.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  